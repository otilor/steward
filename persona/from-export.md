# From Claude export

_No export ingested yet._

Place a Claude `conversations.json` (or unzipped export folder) and run:

```
npm run ingest -- /path/to/conversations.json
```

This file will be replaced with a short summary of recurring topics, books, prayer language, startup names, and how you actually talk. The live prompt includes this _after_ the constitution — never the raw dump.
